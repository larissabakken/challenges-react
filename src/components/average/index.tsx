export default function Average({ numbers = [1, 2] }) {
  // Use the useMemo hook to calculate the average of the numbers
  console.log(numbers)
  return <div>{/* Display the result of the average of the numbers */}</div>
}
